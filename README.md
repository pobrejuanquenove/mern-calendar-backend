# Backend MERN Calendar

## Dev

1. Install dependecies `npm install`
2. Clone `env.template` and rename it to `.env`
3. Update the environment variables used in the `.env`
4. Start up the the server by running `npm start` 